@model PMS.WebUI.Models.ProductInventoryViewModel.ShipmentModel

<div class="modal fade" id="shipmentModal" tabindex="-1" role="dialog" aria-labelledby="shipmentModal" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLongTitle">Receive Shipment</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="container-fluid">
                    <form asp-controller="Inventory" asp-action="UpdateUnitsAvailable" method="post">
                        <div class="form-group">
                            <label class="font-weight-bold">Choose Product</label>
                            <select asp-for="Id" asp-items="Model.ProductsListItem"  class="form-control">
                                <option>Choose a product</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label class="font-weight-bold">Quantity Received</label>
                            <input asp-for="Adjustment" class="form-control" />
                        </div>

                        <div class="modal-footer">
                            <button class="btn btn-primary btn-sm" type="submit">Save Received Shipment</button>
                            <button class="btn btn-secondary btn-sm" data-dismiss="modal">Close</button>
                        </div>
                    </form>
            </div>
        </div>
    </div>
</div>